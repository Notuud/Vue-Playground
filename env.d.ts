// Provide Vite's client ambient types to the TypeScript compiler.
// This gives access to `import.meta.env` typed declarations and other
// Vite-specific globals inside the project. Keep this file at repo root
// (or referenced in `tsconfig.app.json`) so editors like Volar pick it up.
/// <reference types="vite/client" />
